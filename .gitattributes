# Git LFS configuration for Repo-Asset handling
# Prevent the 12,500 JAX agents from timing out while pulling heavy textures
*.amber filter=lfs diff=lfs merge=lfs -text
*.texture filter=lfs diff=lfs merge=lfs -text
