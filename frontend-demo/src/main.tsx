import React from "react"
import ReactDOM from "react-dom/client"
import { NexusLegacyApp } from "../../frontend/NEXUS_LEGACY_APP"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <NexusLegacyApp />
  </React.StrictMode>
)
