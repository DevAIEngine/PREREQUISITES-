Manus, to optimize our token usage and workflow, I am authorizing you to complete the final orchestration steps. Please execute all of the following requirements in one continuous deployment:
1. The Analytics Agent & SEO Integration
* Do not build a manual analytics dashboard. Instead, build the automated Analytics Agent from Phase 4 of the master blueprint.
* Automate YouTube Data Ingestion: Set up a weekly APScheduler job. 48 hours after any video is published via the YouTube Data API v3, retrieve the video's view count, watch time, and audience retention metrics, and append that data to our Google Sheets Logging Layer.
* TubeBuddy SEO Integration:  Manus, write a Python initialization script using the Google Drive API to programmatically download the TubeBuddy and TubeRanker SEO guides and upload them directly to our Drive Asset Layer. Then, configure the google.generativeai wrapper to permanently read those files as its System Instructions.
* Optimized Metadata Generation: Ensure that when Gemini generates the snippet.title, snippet.description, and snippet.tags for the final YouTube upload, it mathematically applies the TubeBuddy SEO rules it read from Drive. Email me an automated prompt optimization report via the Gmail API.
2. Implement "Static Fallback Mode" (Distributed Resilience)
* If the Veo API (vertexai.preview.vision_models) exhausts its MAX_VEO_RETRIES = 3 limit, do not pause the pipeline. Catch the error, log the timeout to our Google Sheets Error Log, and trigger a new Static_Fallback_Assembly function.
* Read the completed TTS audio metadata from the Google Sheets Narration Library index. Script FFmpeg to pull the user's Drive photos and selected PBS/Nat Geo B-roll. Apply a slow zoom/pan (Ken Burns effect), looping them to perfectly match the duration of the TTS audio track.
* Append fallback_assembly_used: true to the Project Manifest JSON.




3. Integrate Orchestration Routes & Sovereign Mode Pre-Flight
Wire all modules into the /api/orchestration/publish endpoint to run sequentially: Decompose → Generate (Veo or Fallback) → Assemble (FFmpeg) → Pre-Flight Validation → Deposit to Drive → Publish to YouTube.
Sovereign Mode: Insert a Sovereign Mode Pre-Flight Validation step before executing the Drive overwrite and YouTube publish. If validation fails, pause the pipeline, log the flag to Sheets, and use the Gmail API to send an admin notification.
4. Add Project Status Polling (The Manifest State Machine)
Create the /api/orchestration/status/:projectId endpoint. This route must strictly read the current state from the Project Manifest JSON stored in Google Drive, ensuring the engine is stateful at the project level so users can resume dropped sessions.
5. Implement Webhook Notifications, Live Audio & Final Deliverables
Frontend Webhooks: Upon successful publication, trigger the React "Success Screen" displaying the YouTube link, share buttons, and a "Create Another Story" button.
Gmail Notification: Wire the Gmail API to send a final success notification to the user containing a Gemini-generated summary of their project.
WebSocket Upgrade: Upgrade our 'Record Your Story' component to support a live WebSocket audio stream to the Gemini API. When the user completes their interview, save the transcript to context.conversation_history in the Project Manifest, and instantly pass it to the Scene Decomposer.
6. Google AI Studio UI Integration & Handoff
I will use Google AI Studio ("Vibe Coding") with Gemini 3 to rapidly build and refine our 7-Step "Story Capture Flow" (Onboarding, Story Capture, Media & B-Roll, Voice & Language Selection, Review & Publish).
Enforce Accessibility: The AI Studio code will enforce our strict rules: 48px minimum touch targets, WCAG AAA high contrast (7:1), and the Voice Guidance System speaker icon positioned in the top-right of every screen.
The Handoff: I will hand you the finalized React 19, TypeScript, and Tailwind CSS code generated by AI Studio. You must replace our current frontend with this code and wire the new buttons directly into our Python backend Orchestration Engine.
7. Affiliate Thumbnail Strategy & Creator Resources
Affiliate Links: Replace all planned Imagen or Pixelbin integrations on the "Review & Publish" and "Success" screens. Add a button: "Create Custom Thumbnails with Pixlr (Free)" routed to my Pixlr affiliate link, and a secondary button: "Generate AI Cinematic Art with ImagineArt" routed to my ImagineArt affiliate link.
Creator Resources Section: In the "YouTube Channel" UI tab, add a "Creator Growth" sub-section containing helpful links to YouTube Studio Analytics, Google Trends, and TubeBuddy to help creators understand retention and CTR.
8. Trend-Based Topic Ideation Module
Add a "Topic Ideas" Module: On the "Record Your Story" screen, add a secondary button titled "Need an idea?".
Wire the Ideation Route: Trigger a new backend route using the Gemini API acting as a YouTube Trends analyst. Generate 3 simple, engaging story prompts based on historically popular YouTube search trends (e.g., "Life in the 1960s").
Conversational Integration: Ensure these 3 topics are displayed in 18pt minimum large text and are read aloud by the Voice Guidance System when the user taps the speaker icon, maintaining our strict "No jargon" rule.