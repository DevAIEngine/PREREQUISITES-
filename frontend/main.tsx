import React from 'react'
import ReactDOM from 'react-dom/client'
import { NexusLegacyApp } from './NEXUS_LEGACY_APP.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <NexusLegacyApp />
  </React.StrictMode>,
)
